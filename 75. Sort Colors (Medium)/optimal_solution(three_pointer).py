class Solution:
    def sortColors(self, nums: List[int]) -> None:
        """
        Do not return anything, modify nums in-place instead.
        """
        low = 0
        curr = 0
        high = len(nums) - 1
        
        while curr <= high:
            if nums[curr] == 0:
                # 發現 0，換到左邊
                nums[low], nums[curr] = nums[curr], nums[low]
                low += 1
                curr += 1
            elif nums[curr] == 2:
                # 發現 2，換到右邊
                nums[curr], nums[high] = nums[high], nums[curr]
                high -= 1
                # 這裡不移動 curr，因為換過來的數可能是 0 或 1，需要下一輪檢查
            else:
                # 發現 1，保持原位
                curr += 1
'''
### 💡 My Notes (解題重點)

這題的最佳解法是 **One-pass (一次遍歷)** 的三指標法：

1. **三個指標**：
* `low`：指向下一個 `0` 應該存放的位置（左側邊界）。
* `curr`：當前正在檢查的元素位置。
* `high`：指向下一個 `2` 應該存放的位置（右側邊界）。


2. **移動邏輯**：
* 若 `nums[curr] == 0`：與 `nums[low]` 交換，`low` 和 `curr` 都往右移一位。
* 若 `nums[curr] == 1`：不需移動，`curr` 往右移一位即可。
* 若 `nums[curr] == 2`：與 `nums[high]` 交換，`high` 往左移一位。**注意：此時 `curr` 不動**，因為從右邊換過來的數字還沒檢查過。

### 📊 複雜度分析

* **時間複雜度**：，只需要遍歷陣列一次。
* **空間複雜度**：，直接在原陣列上進行交換。

'''
